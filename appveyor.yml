before_build:
  - nuget restore
branches:
  only:
    - master
build:
  project: EasyCrypto.sln
  verbosity: minimal
image: Visual Studio 2015
configuration: Release
test:
  assemblies: 
    - EasyCrypto.Tests.dll